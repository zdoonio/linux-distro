#!/bin/bash
# Ustaw uprawnienia (jeśli zapomniałeś)
chmod +x /usr/local/bin/ai_assistant.py

# Ewentualnie ustaw xfconf (jeśli wolisz)
# xfconf-query -c xfwm4 -p /general/theme -s LunaXPTP
# xfconf-query -c xsettings -p /Net/IconThemeName -s LunaIcons